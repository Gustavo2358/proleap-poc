# EP-W5 — generic, hypothesis-resistant qualification

Status: **GREEN — READY FOR HUMAN REVIEW** (Draft PRs remain Draft).
REAL CASE = NOT AVAILABLE.

## What this wave proves

The preservation rule is exercised independently of historical keyword spellings.
The typed AST metamorphism replaces a declaration clause with arbitrary JOHNDOE;
it does not add grammar support or declare preserved clauses harmless. Twelve
factor combinations (encoding profile × physical offset knowledge × no operand /
USING / RETURNING) each run baseline and partial variants. The same supported
PROGA survives source/SP, AIR entry/target, RD BEFORE, Regional Values BEFORE,
DependencySite support/remainder and both final JSON readers. A strong baseline
may become a possible entry with remainder; support cannot disappear.

Independent typed AIR contrasts add visible/outcome effects, open invocation and
Diverge control, unknown object binding, unavailable unrelated unit, and an unproved
codec write. The latter remains INCOMPLETE_VALIDATION. Only explicit partial
admission with complete operation-owned precondition scope is allowed; the affected
operation loses MUST authority. Strict codec and projection policies still refuse.
Partial projection retains the original Publication and every occurrence, typed
known edges and conservative lazy unknown edges in the existing context/solver.
Unknown edges never target the entry seed node. No second solver is introduced.

Additional source verticals cover unknown statements/arguments/results, branch
MAY and executed branch MUST, missing input in other units with literal DIRECT,
and missing VALUE. Frozen historical sources cover USING, DISPLAY, SYNC,
SYNCHRONIZED, JUSTIFIED, BLANK WHEN ZERO, SIGN, two possible VALUEs with unknown
separation, computed/literal sites and exact overwrite.

## Counterproofs that changed the implementation

1. Four independent additions originally removed computed A through global scalar
   or CFG preparation refusal (`independent-contrasts-red-02`). Partial projection
   and the existing regional provider retain it without a second execution engine.
2. A known unrelated operation with unproved codec preconditions caused global
   refusal (`semantic-admission-red-01`). Normative scoped partial admission was
   specified before APIs; status remains incomplete, and its writes are MAY.
3. After the first W5 FAST closure, an unknown-size prefix before a sending text
   item erased a derived PROGA (`risk-vertical-02`). First loss: source → SP MOVE
   relation. SP 2.20/storage 1.7 now preserves a proved logical whole-text copy when
   physical source offset is absent and source/destination separation is positively
   proved. Lower emits existing Assign/FitText/Read and explicit PARTIAL output.
4. The independent regional reader rejected the new support because a partial
   Assign was being serialized as an unconditional literal producer. Regional JSON
   1.3 explicitly carries BEFORE logical capture and source support, without a
   physical source range; destination materialization keeps remainder.
5. Removing VALUE from the same copied-source control exposed a lower identity
   dependency and NPE (`copy-controls-01`). Logical object materialization now also
   follows the positively admitted copy relation. No VALUE means zero invented
   candidate; DIRECT still survives (`copy-controls-02`).

These are counterexamples beyond the original SYNC mechanism. Failed/setup runs
remain available and are not counted as GREEN. See COPY-COUNTERPROOF.md.

## Laws and executable authority

| Law | Independent executable protection |
| --- | --- |
| M1 UNKNOWN cannot reduce supported recall | EvidenceMetamorphicTest; EvidenceMonotonicityTest; EvidencePreservingEntryTest; source→JSON generic pairs |
| M2 precision monotonicity | strong→possible profile contrast; unknown offset/base; closed/open source-copy layout pair; explicit remainder and support checks |
| M3 MUST authority | KillAuthorityTest; EvidencePreservingPolicyTest; exact overwrite after MAY/unproved write; branch MUST and copied-value MUST controls |
| M4 information addition | argument/result pairwise cases; unrelated unknown declaration/object/unit; effect/outcome additions; typed target reads preserved |
| M5 failure containment | PartialDependencyTest; partial codec CLI tests; input-gap multiple-unit/DIRECT verticals; semantic partial primary dependencies.json |

Required risky triples are covered: VALUE × partial construct × CALL USING;
VALUE × MAY/MUST × branch; computed target × arguments/results × JSON;
input gap × multiple units × literal CALL. Positive invalidity, no VALUE, old wire
versions, unknown alias, unsupported source slices and unrepresentable codec are
contrasts, not cases silently coerced into success.

## Policy mutation and transport evidence

`policy-mutants-04/results.json`: six semantic mutants KILLED, no setup/compile
error counted: UNKNOWN→clear; MAY→kill; unproved alias→kill; query-time VALUE
reseed; unknown storage→empty; unproved operation precondition→strong kill.
The runner restores source in finally. `unmutated-build-02.log` rebuilt clean
classes after mutation. Regional logical-capture reader kills 10 additional wire
mutants; previous logical entry reader kills eight and partial dependency reader
eight. Reader acceptance alone does not prove source semantics: handwritten
candidate/BEFORE/support/remainder assertions accompany each vertical.

## Lean gate decision

Required evidence = delta regression ∪ relevant product laws ∪ incident contrasts.
One stable FAST closed W1 for each production repo; W2 and W4 changed only CFG,
W3 only source. W5 initially closed FAST, then the independent copy counterproof
invalidated source/lower/codec/values evidence and required another closure.
Frontend retry corrected two explicit version assertions after SP/storage evolution.
Lower's final retry is justified by the no-VALUE materialization counterproof.
CFG's earlier retry corrected an explicit test inventory after all Java tests passed.
Final cfg-fast-03 passed the semantic tests/readers, but javap captured a JVM
`hsperfdata` file-lock warning during concurrent isolated builds. This polluted an
API descriptor and caused inventory drift. A standalone refresh produced exactly
the committed W4 inventory (no relaxation); cfg-fast-04 repeats the gate alone.

NEW final logs and selected verticals are listed in HANDOFF.md. REUSED: unchanged
W1 model and strong-initial contrast families, W2 laws, W3 scoping, W4 coexistence
and partial-site wire controls, each with its original heads/logs. NOT RERUN: all-repo
full, CardDemo73, complete historical mutations, complete Storage/CICS/DVI25 campaigns.
No broad corpus was used as a debugger.

## What this does not prove / independent remaining causes

This qualifies the tested architectural incident class, not every COBOL construct
or every codec expression. The parser must first publish a valid recognizable
source fact. Invalid literals, missing input containing the declaration, structural
invalidity and unsupported/unnegotiated model forms can still prevent analysis.
Resource exhaustion remains an explicit failure, never certified success.

Unknown allocation/alias may still open a whole unit when no narrower bound is
proved. A logical copy needs positive separation and known textual shape; slices,
multiple receivers and unproved alias do not acquire invented copy precision.
The generic AST JOHNDOE experiment proves downstream behavior, not grammar support
for a literal COBOL JOHNDOE keyword. Primary SP export selects a program unit;
multiple-unit input-gap tests prove isolation of the selected unit, not aggregate
primary export of every unit. Legacy CFG JSON v1–v3 cannot encode the new contextual
partial projection and explicitly refuse it; primary dependency JSON supports it.

Independent causes still capable of the symptom include a missing source fact,
unsupported target interpretation and a real complete executed overwrite. Tests
separate those from loss caused only by unrelated uncertainty.

Qualification: LOCAL FIX and ARCHITECTURAL LAW proved for the stated domains;
INCIDENT CLASS qualified by generic families plus historical contrasts, with final gates passed. REAL CASE = NOT AVAILABLE. No 4/4 claim. No merge/auto-merge.

## Final executable closure (2026-09-16)

- Source FAST `source-fast-03.log`: PASS, 335 tests, zero failures/errors/skips.
- AIR FAST `air-fast-02.log`: PASS, model/validator/codec and harness checks.
- Lower FAST `lower-fast-04.log`: PASS after the no-VALUE counterproof; 131 seconds.
- CFG FAST `cfg-fast-04.log`: PASS, 470 tests, zero failures/errors/skips, plus
  architecture and independent readers; 77 seconds.
- Spec changed Markdown links and diff check: PASS (18 local links); bilateral AIR
  tests exercise normative obligations. No fictitious production FAST for spec.
- Final fresh CLI verticals: 24 generic + 12 risk + 13 historical = **49 PASS**;
  `final-{generic,risk,historical}-01/boundaries.json`; no first lost boundary.
- Policy mutations: **6/6 killed**, semantic assertions only.
- Final original-checkout hygiene: all clean, HEAD=origin/main=recorded baseline.
  GitHub query: only the five persistent EP Draft PRs are open.
- Runtime jar hashes, exact production heads and documentation-only working diff:
  `final-vertical-provenance.json`; raw logs remain in the campaign workspace.

The no-VALUE, truncation, capture-before-source-write and later MUST controls passed.
Every mutated source was restored, and unmutated classes were rebuilt before final
qualification. Further reruns require new invalidated evidence, not repetition for
appearance. Human review is the next step; no merge or auto-merge is authorized.

## Qualified repository snapshot

Production/pin closure: `b5f626f5b42b5168af29a740aace60a1f0a993d1`. This final documentation commit does not change runtime semantics.
Raw log paths above are relative to the campaign `w5/` directory in the shared workspace.
