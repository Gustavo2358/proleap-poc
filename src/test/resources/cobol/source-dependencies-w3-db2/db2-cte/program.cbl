       IDENTIFICATION DIVISION.
       PROGRAM-ID. DB2PGM.
       DATA DIVISION.
       WORKING-STORAGE SECTION.
       01 WS-SQL PIC X(80).
       PROCEDURE DIVISION.
       EXEC SQL
       WITH ATIVOS AS (SELECT * FROM CLIENTE) SELECT * FROM ATIVOS
       END-EXEC.
       GOBACK.
