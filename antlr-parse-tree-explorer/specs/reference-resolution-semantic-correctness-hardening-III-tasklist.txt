# Reference Resolution and Call Semantics Correctness Hardening III

## Objetivo

Executar uma rodada conservadora de hardening na fronteira entre resolução nominal de PROGRAM,
compiler options de CALL e produtos de análise. Os achados do review são hipóteses até serem
comprovados por regra documentada, fixture mínima, teste executável e RED observado.

Esta rodada não introduz CFG, call graph, def-use ou reaching definitions.

## Método obrigatório

HIPÓTESE
→ REGRA IBM / CONTRATO LOCAL
→ FIXTURE ADVERSARIAL MÍNIMA
→ TESTE ANTES DA CORREÇÃO
→ RED COMPROVADO OU HIPÓTESE REFUTADA
→ CAUSA RAIZ
→ CORREÇÃO GERAL MÍNIMA
→ GREEN ESPECÍFICO
→ SUÍTE COMPLETA
→ COMMIT ISOLADO

Nenhum comportamento será alterado apenas porque o review parece plausível. Nenhuma correção
pode depender da grafia ou do caminho de uma fixture. Resultados incertos permanecem
`UNSUPPORTED`, `UNRESOLVED` ou `AMBIGUOUS`; nunca selecionar arbitrariamente o primeiro target.

Cada correção funcional deve terminar em commit próprio. Achados novos terão teste RED e commit
adicional separado. Não alterar gramáticas, corpus ou outputs versionados sem necessidade
demonstrada pelo contrato.

======================================================================
FASE 0 — BASELINE E ESCOPO
======================================================================

STATUS: [x] CONCLUÍDA

- SHA inicial: `cbe1b365aad74f8ce3b7f2ed3eb084c2fb5aa567`.
- Worktree limpo e sincronizado com `origin/main`.
- Maven: 78 testes, 0 failures, 0 errors, 0 skipped.
- JavaScript: 31 arquivos verdes em `node --check`.
- `git diff --check`: verde.
- Documentação primária consultada:
  - IBM Enterprise COBOL 6.4 — PGMNAME;
  - IBM Enterprise COBOL 6.4 — DYNAM;
  - IBM Enterprise COBOL 6.4 — static e dynamic calls.

Commit: `docs: record call-semantics hardening baseline`

======================================================================
FASE 1 — PGMNAME AUSENTE NÃO PODE PUBLICAR IDENTIDADE EXTERNA FALSA
======================================================================

STATUS: [x] CONCLUÍDA — RED confirmou false certainty; comparação por IDs catalogados verde

Hipótese:

`dependsOnPgmname` não reconhece nomes alfanuméricos uppercase que começam por dígito. Sob
`UNSPECIFIED`, `CALL '1PROG'` pode consultar uma única chave e publicar certeza, embora COMPAT e
LONGUPPER produzam `APROG` e LONGMIXED produza `1PROG`.

Fixture/teste:

- criar programa mínimo com `CALL 'SIMPLE'` e `CALL '1PROG'`;
- catálogo adversarial deve distinguir `APROG` de `1PROG`;
- provar que `SIMPLE` é option-independent;
- provar que `1PROG` não pode produzir `RESOLVED` sob PGMNAME ausente;
- controles explícitos COMPAT, LONGUPPER e LONGMIXED;
- verificar `SemanticEntityId`, candidates e reason.

Correção somente após RED:

- eliminar heurística lexical;
- derivar as identidades suportadas e comparar decisões/targets possíveis;
- preservar lookup indexado e candidates possíveis quando houver divergência.

Commit: `fix: make unspecified external pgmname conservative`

======================================================================
FASE 2 — SEPARAR FORMA SINTÁTICA DE CALL E LINKAGE SEMÂNTICO
======================================================================

STATUS: [x] CONCLUÍDA — RED de quatro contratos e transporte ponta a ponta; 81 testes verdes

Hipótese:

`CallTargetKind.STATIC_LITERAL` afirma linkage estático a partir da forma literal. IBM documenta
que CALL literal é dinâmico com DYNAM e estático com NODYNAM; CALL identifier é dinâmico em ambos.

Fixture/teste:

- CALL literal e CALL identifier sob DYNAM, NODYNAM e opção ausente;
- provar transporte estrutural das opções do preprocessador para a policy;
- provar que a AST representa apenas `LITERAL_PROGRAM_NAME` versus `IDENTIFIER_OR_EXPRESSION`;
- expor linkage `STATIC`, `DYNAMIC` ou `UNKNOWN` no produto apropriado;
- nested program literal deve continuar nominalmente resolvível e não depender de DYNAM;
- remover/renomear métricas que chamam literal de static.

Correção somente após RED:

- modelar `DynamMode` na policy;
- modelar separadamente target syntax e call linkage;
- ausência da opção efetiva permanece `UNKNOWN`, não default inventado;
- preservar identidade estrutural e AST imutável.

Commit: `fix: separate call target syntax from linkage`

======================================================================
FASE 3 — CALL EXTERNO DINÂMICO USA IDENTIDADE DINÂMICA, NÃO PGMNAME ESTÁTICO
======================================================================

STATUS: [x] CONCLUÍDA — RED selecionou target estático sob DYNAM; chave por linkage e incerteza verdes

Hipótese:

O catálogo externo é sempre consultado por `ProgramNameCanonicalizer.external(..., pgmnameMode)`.
IBM documenta que PGMNAME não afeta dynamic calls; a identidade dinâmica usa folding uppercase,
truncamento em oito e traduções compatíveis.

Fixture/teste:

- CALL literal sob DYNAM + PGMNAME(LONGMIXED/LONGUPPER);
- nome longo, hífen e leading digit que diferenciem a chave estática da dinâmica;
- NODYNAM deve continuar usando PGMNAME explícito;
- CALL identifier permanece target dinâmico em runtime e não vira certeza nominal por valor;
- opções ausentes devem comparar todos os resultados semanticamente possíveis.

Correção somente após RED:

- centralizar canonicalização dinâmica explícita;
- escolher a chave pela linkage conhecida;
- quando linkage/PGMNAME forem desconhecidos, comparar targets catalogados possíveis;
- nenhuma varredura por referência sobre todo o catálogo.

Commit: `fix: canonicalize external calls by linkage`

======================================================================
FASE 4 — CONTRATO DE REDEFINES/RENAMES É BINDING NOMINAL/ESTRUTURAL
======================================================================

STATUS: [x] CONCLUÍDA — binding funcional preservado; escopo nominal/estrutural explicitado

Hipótese:

O comportamento está correto, mas `DeclarationRelationResolution.RESOLVED` pode ser interpretado
como validação integral da cláusula COBOL.

Teste de contrato:

- usar fixtures estruturais existentes;
- provar que RESOLVED identifica target(s) nominal/estrutural(is);
- provar que o produto não afirma validação semântica completa da cláusula;
- se o contrato já for inequívoco, limitar mudança a Javadocs e teste.

Commit: `test: define declaration relation binding contract`

======================================================================
FASE 5 — MÉTRICAS DEVEM DISTINGUIR HINT SINTÁTICO DE KIND RESOLVIDO
======================================================================

STATUS: [x] CONCLUÍDA — RED confirmou métrica ambígua; contagens sintática e semântica separadas

Hipótese:

`ResolutionAnalysisReport.kindCounts` conta `occurrence.kind()` e pode apresentar DATA resolvido
como INDEX em subscripts polimórficos.

Fixture/teste:

- reutilizar `subscript-semantic-kind.cbl`;
- provar a divergência entre hint sintático e candidate final;
- renomear a métrica existente para `syntacticKindCounts`;
- adicionar `resolvedSemanticKindCounts` somente para entries RESOLVED;
- snapshots/UI devem rotular ambas sem quebrar o contrato de binding.

Commit: `fix: distinguish syntactic and resolved kind metrics`

======================================================================
FASE 5A — CALL INCERTO DEVE BLOQUEAR DEPENDENCY READINESS, NÃO BINDING NOMINAL
======================================================================

STATUS: [x] CONCLUÍDA — bug novo do review final; dois REDs e regressão verdes

Achado adicional:

- target externo pode ser nominalmente certo sob todas as opções e ainda ter linkage UNKNOWN;
- CALL identifier pode resolver corretamente a variável DATA sem conhecer o valor de programa em runtime;
- nenhum desses casos pode autorizar call/dependency analysis completa.

Fixtures/testes:

- `unknown-linkage-same-external-target.cbl` prova target certo com linkage desconhecido;
- `call-identifier-runtime-target.cbl` prova binding DATA certo sem target de programa conhecido;
- `CallSemanticsTest` exige separação entre `referenceBindingComplete` e
  `dependencyAnalysisReady` e gaps específicos.

Correção:

- `CALL_LINKAGE_UNKNOWN` e `DYNAMIC_CALL_TARGET_VALUE_UNKNOWN` em `CALL_SEMANTICS`;
- bindings nominais certos permanecem completos;
- dependency readiness permanece falsa até a informação ausente existir.

Commit: `fix: block dependency readiness on unknown call semantics`

======================================================================
FASE 5B — NODYNAM NÃO PROVA STATIC SEM NODLL
======================================================================

STATUS: [x] CONCLUÍDA — bug novo do review final; DLL/NODLL transportado e linkage explícito

Achado adicional:

IBM define static CALL literal sob NODYNAM e NODLL. NODYNAM com DLL não pode ser publicado como
STATIC. A policy e o preprocessor não transportavam DLL/NODLL e o vocabulário não representava
linkage DLL.

Fixture/teste:

- `call-linkage-dll.cbl` com `CBL NODYNAM,DLL`;
- controles DYNAM/NODLL e NODYNAM/NODLL nas fixtures da Fase 2;
- provar `DllMode` no outcome/policy e `CallLinkage.DLL` na entry.

Correção:

- `DllMode {DLL,NODLL,UNSPECIFIED}`;
- STATIC exige NODYNAM+NODLL;
- NODYNAM+DLL resulta DLL;
- DLL ausente permanece UNKNOWN;
- canonicalização DLL permanece governada por PGMNAME.

Commit: `fix: model dll linkage for literal calls`

======================================================================
FASE 5C — DYNAM+DLL INVÁLIDO NÃO PODE PUBLICAR CERTEZA
======================================================================

STATUS: [x] CONCLUÍDA — bug novo do review final; configuração inválida fica UNSUPPORTED

Fixture `invalid-dynam-dll-call.cbl` e teste adversarial comprovam que a combinação proibida pela
IBM antes produzia target RESOLVED/linkage DYNAMIC. O resolver agora retorna
`UNSUPPORTED_DIALECT_OPTION` e linkage UNKNOWN. DYNAM sem DLL/NODLL explícito também preserva
linkage UNKNOWN; somente DYNAM+NODLL prova DYNAMIC.

Commit: `fix: reject invalid dynam dll call semantics`

======================================================================
FASE 6 — EVIDÊNCIA DOCUMENTAL E REGRESSÃO INTEGRADA
======================================================================

STATUS: [ ] PENDENTE

- corrigir no relatório anterior o SHA inicial inválido para
  `58aea33692984750b647d87471f7f7fdc26ef4cb`;
- registrar o erro como documental, sem reescrever o histórico;
- reexecutar individualmente todos os novos adversariais;
- executar Maven completo, JavaScript, anti-scan, testes desabilitados e `git diff --check`;
- confirmar áreas protegidas e desempenho/indexação;
- tabela RED/correção/GREEN/commit.

Commit documental do SHA: `docs: correct second hardening baseline sha`

======================================================================
FASE 7 — REVIEW FINAL E RELATÓRIO
======================================================================

STATUS: [ ] PENDENTE

Revisar no código:

- `ProgramNameCanonicalizer`, `CobolReferenceResolver` e catálogo externo;
- AST/collector para CALL literal e identifier;
- `PreprocessorEngine`, `CobolResolutionPolicy`, DYNAM/NODYNAM/PGMNAME;
- `ReferenceResolution`, snapshots e métricas;
- `DeclarationRelationResolution` e readiness;
- complexidade dos índices e ausência de false certainty.

Atualizar `reference-resolution-semantic-correctness-report.md` com baseline, hipóteses, REDs,
causas raiz, commits, GREEN, performance, lacunas e conclusão de readiness. A conclusão deve
distinguir explicitamente:

- readiness de binding nominal/CFG estrutural;
- readiness de call graph externo;
- features futuras não iniciadas.

Commit: `docs: finalize call-semantics hardening`

======================================================================
CRITÉRIOS FINAIS DE ACEITE
======================================================================

STATUS: [ ] PENDENTE

- cada hipótese executada contra o estado pré-correção;
- cada bug confirmado possui RED preservado e regressão permanente;
- hipótese refutada registrada sem mudança funcional desnecessária;
- commits de correções separados;
- nenhuma opção ausente convertida em certeza;
- sintaxe literal não implica linkage estático;
- canonicalização externa respeita linkage;
- contrato de relation binding explícito;
- métricas sintática e semântica não se confundem;
- SHA documental corrigido;
- Maven, JavaScript, anti-scan e integridade verdes;
- review final executado;
- nenhum CFG, call graph, def-use ou reaching definitions implementado.
